# Codex Harness Integration

This integration lets `vitadex` coordinate Codex-owned low-level agent sessions while the local runtime keeps ownership of memory, tasks, approvals, logs, follow-ups, dashboard, communication formatting, and safe mode.

Default behavior is dry-run and fail-closed. Real Codex execution is intentionally not implemented in safe mode.
